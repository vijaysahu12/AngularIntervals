import { LastCharUperCasePipe } from './last-char-uper-case.pipe';

describe('LastCharUperCasePipe', () => {
  it('create an instance', () => {
    const pipe = new LastCharUperCasePipe();
    expect(pipe).toBeTruthy();
  });
});
